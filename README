# AgroVisão AI: Detecção de Doenças em Plantações com IA

Um sistema inteligente que utiliza Deep Learning para a detecção e classificação precoce de doenças em culturas agrícolas a partir de imagens.

## 🎯 Sobre o Projeto

A agricultura enfrenta perdas anuais significativas devido a pragas e doenças. O monitoramento manual é lento, caro e sujeito a falhas. Este projeto é uma Prova de Conceito (POC) de uma solução de Inteligência Artificial, a **"AgroVisão AI"**, que visa automatizar esse processo.

Utilizando imagens capturadas por drones ou smartphones, o sistema analisa, identifica e mapeia a incidência de doenças, permitindo que agricultores tomem ações rápidas e direcionadas, otimizando o uso de recursos e aumentando a produtividade.

## ⚙️ Arquitetura da Solução

O fluxo do sistema foi modelado para ser simples e eficiente, desde a coleta de dados em campo até a visualização do diagnóstico pelo usuário.

```mermaid
graph TD
    A[1. Coleta de Dados: Drone/Smartphone] --> B{2. Upload de Imagens};
    B --> C[3. Plataforma AgroVisão AI];
    C --> D{4. Processamento de Imagem};
    D --> E[5. Modelo de IA (CNN)];
    E --> F[6. Análise e Classificação];
    F --> G[7. Geração de Resultados];
    G --> H[8. Dashboard do Agricultor];

    subgraph "Plataforma na Nuvem"
        C; D; E; F; G;
    end
    subgraph "Campo"
        A; B;
    end
    subgraph "Usuário Final"
        H;
    end
```

### Fluxo de Dados

  * **Entradas:**

      * Imagens digitais da plantação (folhas, caules) em alta resolução.
      * (Opcional) Metadados de geolocalização (GPS) associados a cada imagem.

  * **Processamento:**

    1.  **Pré-processamento:** A imagem é normalizada e segmentada para isolar a área de interesse.
    2.  **Inferência:** A imagem é processada por uma Rede Neural Convolucional (CNN) treinada para identificar padrões de doenças.
    3.  **Classificação:** O modelo retorna a classe mais provável (ex: "Saudável", "Ferrugem") com um grau de confiança.
    4.  **Agregação:** Dados de GPS, se disponíveis, são usados para gerar um mapa de calor da incidência.

  * **Saídas:**

      * Diagnóstico da imagem com o nome da doença.
      * Grau de confiança da predição.
      * Mapa visual da plantação destacando as áreas afetadas.

## 🛠️ Tecnologias Utilizadas

Este projeto foi construído utilizando as seguintes tecnologias:

  * **Linguagem:** **Python**
  * **Framework de Deep Learning:** **TensorFlow/Keras**
  * **Processamento de Imagem:** **OpenCV**
  * **Backend (API):** **FastAPI**
  * **Frontend (Dashboard):** **Streamlit**

## 🚀 Como Executar a POC

Para executar esta Prova de Conceito em seu ambiente local, siga os passos abaixo.

### Pré-requisitos

  * Python 3.9+
  * Git

### Instalação

1.  Clone o repositório:

    ```sh
    git clone https://github.com/seu-usuario/agrovisao-ai.git
    cd agrovisao-ai
    ```

2.  Crie e ative um ambiente virtual:

    ```sh
    # Windows
    python -m venv venv
    .\venv\Scripts\activate

    # Linux / macOS
    python3 -m venv venv
    source venv/bin/activate
    ```

3.  Instale as dependências:

    ```sh
    pip install -r requirements.txt
    ```

### Execução

1.  Inicie a aplicação com Streamlit:
    ```sh
    streamlit run app.py
    ```
2.  Abra seu navegador e acesse `http://localhost:8501`.
3.  Faça o upload de uma imagem de uma folha de planta para ver o diagnóstico.

## 🗺️ Próximos Passos

Este projeto é uma POC funcional. Os próximos passos para evoluir a solução incluem:

  - [ ] Aumentar o dataset com mais imagens e classes de doenças.
  - [ ] Otimizar o modelo de IA para maior acurácia e performance.
  - [ ] Desenvolver um sistema de autenticação de usuários.
  - [ ] Integrar com APIs de clima e dados de solo para enriquecer as predições.
  - [ ] Criar um aplicativo mobile para facilitar a captura e envio de imagens.
